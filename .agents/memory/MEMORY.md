- [Autoscale deploy startup/health-check](deployment-autoscale-startup.md) — publish "build failed" is often a promote/health-check failure; bind the port before any awaited DB work, and re-publish to apply dev→prod schema drift.
- [PDF visual verification](pdf-visual-verification.md) — render jsPDF sample + pdftoppm to PNG to check invoice/receipt layout without a browser.
- [Appraisals grade model](appraisals-grade-model.md) — Designation (grades) vs Grade (salaryGradeBands); current salary = grade Basic, snap within designation; grade-sheet import must prune only incoming designations; no FK on users.gradeBandId (schema order).
- [Statement of Account is a payment ledger](statement-of-account-ledger.md) — drop 0/0 rows, installment lines use CR title, expand split payments (aggregate only as fallback), keep table/PDF/CSV coherent.
- [express-rate-limit + trust proxy](express-rate-limit-trust-proxy.md) — `trust proxy: true` is load-bearing for OAuth; IP middleware must set `validate.trustProxy:false`, don't change proxy trust casually.
- [Milestone/CR-installment payment aggregation](milestone-payment-aggregation.md) — milestones/installments can be paid by MANY split payments; always recompute status/receivedAmount from the full linked set, never single-payment sync.
- [Seeding reference data to prod](seeding-reference-data-to-prod.md) — prod DB is read-only & publish sync is wholesale; use idempotent seed-when-empty in startup, bundle JSON via esbuild import, preserve IDs, resolve child FKs from current DB.
- [Drive document upload policy](drive-document-uploads.md) — invoice/receipt PDFs auto-save fail-soft + idempotent-by-filename; from-payment backfills existing invoices; regenerate-same-number won't replace the Drive copy.
- [RBAC admin fallback (UI vs server)](rbac-admin-fallback.md) — `/api/access/my-permissions` omits the legacy admin-role fallback; UI `canManage` must OR-in role==="admin"/"administrator" or role-admins get the restricted view.
- [CR / sold-upsell shared edit route](cr-upsell-shared-patch.md) — one PATCH /api/change-requests/:id serves BOTH project workspace edit + Upsell sold edit; new CR fields need wiring in two frontends; non-schema fields (tagIds) extracted before zod.
- [Google Drive connector storage](google-drive-connector-storage.md) — per-project Drive folders; needs BOTH addIntegration + proposeIntegration; provisioning must use a global advisory lock (Drive has no unique-name constraint); downloads keyed by domain record, never raw fileId.
- [Sold upsells from locked CRs](sold-upsells-from-locked-crs.md) — sold upsells are a read-only projection of locked change_requests (no dup table); attachmentPath must be canonical /objects/...; don't touch payment recompute.
- [Appraisal rollout undo](appraisal-rollout-undo.md) — rollout snapshots employee's prior grade/band into appraisal so an undo can restore them; restore mirrors rollout's conditional applied-field set; both status-guarded idempotent.
- [Shadcn Sheet width overrides](shadcn-sheet-width-override.md) — shared sheet.tsx variants must use `sm:max-w-*` (not fixed `sm:w-[Npx]`) so a consumer's own `max-w-*` className can win via twMerge.
